{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/api-server build && pnpm --filter @workspace/med-platform build && node ./copy-frontend-public.mjs",
  "outputDirectory": "public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[[...path]].ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html", "methods": ["GET", "HEAD"] }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
